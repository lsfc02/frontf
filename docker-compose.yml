
version: '3.8'
services:
  frontend:
    build:
      context: .  
      dockerfile: Dockerfile
      args:
        - VITE_API_BASE_URL=${VITE_API_BASE_URL}
    container_name: cifal-frontend
    restart: unless-stopped
    ports:
      - "8087:8087" 